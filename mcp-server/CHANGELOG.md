# Changelog

## 0.1.1

- Mark `dist/cli.js` executable in published tarball so `npx` can run the bin.

## 0.1.0

Initial release.

- `get_platform_constitution` — returns the Radius platform constitution.
- `analyze_source_repo` — heuristic source-repo signals (language, datastores, ports, suggested architecture pattern).
- `validate_app_bicep` — checklist validator for `app.bicep`.
